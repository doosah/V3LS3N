# 🚀 Быстрое исправление Railway

## Проблема
Railway пытается запустить `index.html` вместо `server/index.js`

## Простое решение

Я создал файлы конфигурации, которые Railway должен автоматически использовать:
- ✅ `railway.json` - основная конфигурация
- ✅ `nixpacks.toml` - для автоматической сборки
- ✅ `Procfile` - резервный вариант
- ✅ `package.json` в корне - с командой запуска

## Что делать:

1. **Закоммить и запушить изменения** в GitHub:
   ```bash
   git add .
   git commit -m "Fix Railway configuration"
   git push
   ```

2. **Railway автоматически перезапустит** сервис после push

3. **Проверь логи Railway** - должно появиться:
   ```
   🚀 Telegram Bot Scheduler запущен
   ✅ Сервер запущен на порту 3000
   ```

## Если не работает:

### Вариант 1: Найти команду запуска в настройках

1. Открой сервис на Railway
2. Найди любое поле с названием типа:
   - "Command"
   - "Start"
   - "Run"
   - "CMD"
   - "Start Command"
3. Установи туда: `cd server && npm start`

### Вариант 2: Пересоздать сервис

1. Удали текущий сервис
2. Создай новый: **New** → **Service** → **GitHub Repo**
3. Выбери репозиторий
4. Railway должен автоматически использовать `railway.json`

### Вариант 3: Использовать Railway CLI

Если установлен Railway CLI:
```bash
railway up
```

## Проверка

После исправления открой: `https://your-service.railway.app/health`

Должен вернуться JSON:
```json
{
  "status": "ok",
  "date": "...",
  "time": "..."
}
```


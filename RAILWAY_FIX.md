# 🔧 Исправление проблемы Railway

## Проблема
Railway пытается запустить `index.html` как JavaScript файл, потому что не знает, что нужно запускать сервер из папки `server`.

## Решение

### Вариант 1: Настроить Root Directory в Railway (рекомендуется)

1. Откройте проект на Railway
2. Перейдите в Settings → Service
3. Найдите раздел "Root Directory"
4. Установите Root Directory: `server`
5. Сохраните изменения
6. Railway автоматически перезапустит сервис

### Вариант 2: Использовать railway.json в корне

Я создал файл `railway.json` в корне проекта, который указывает Railway:
- Build Command: `cd server && npm install`
- Start Command: `cd server && node index.js`

Railway должен автоматически использовать этот файл.

### Вариант 3: Проверить настройки сервиса

1. Откройте проект на Railway
2. Перейдите в Settings → Service
3. Проверьте:
   - **Build Command**: должно быть `cd server && npm install` или оставить пустым
   - **Start Command**: должно быть `cd server && node index.js` или `npm start` (если в server/package.json)
   - **Root Directory**: установить `server` (если доступно)

## После исправления

После настройки Root Directory или обновления команды запуска:
1. Railway автоматически перезапустит сервис
2. В логах должно появиться:
   ```
   🚀 Telegram Bot Scheduler запущен
   📅 Текущая дата: ...
   ✅ Сервер запущен на порту 3000
   ```
3. Проверьте Health Check: `https://your-service.railway.app/health`

## Если проблема сохраняется

1. Удалите текущий сервис на Railway
2. Создайте новый сервис
3. При создании выберите:
   - Source: GitHub repo
   - Root Directory: `server`
   - Build Command: `npm install`
   - Start Command: `npm start`


# 🚀 Простая настройка Railway через PowerShell

## Вариант 1: Быстрая настройка (рекомендуется)

Запусти скрипт:
```powershell
.\railway-quick-fix.ps1
```

Скрипт автоматически:
1. Проверит Railway CLI
2. Войдет в Railway (если нужно)
3. Установит команду запуска

## Вариант 2: Вручную по шагам

### 1. Войди в Railway
```powershell
railway login
```
Откроется браузер для авторизации.

### 2. Привяжи проект
```powershell
railway link
```
Выбери проект "stunning-manifestation" из списка.

### 3. Установи команду запуска
```powershell
railway variables set RAILWAY_START_COMMAND="cd server && node index.js"
```

### 4. Проверь переменные
```powershell
railway variables
```

## Проверка

После настройки:
1. Railway автоматически перезапустит сервис
2. Проверь логи: `railway logs`
3. Проверь health: открой в браузере `https://your-service.railway.app/health`

## Если Railway CLI не установлен

Установи:
```powershell
npm install -g @railway/cli
```


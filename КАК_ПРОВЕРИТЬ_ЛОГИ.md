# 📊 Как проверить логи Railway

## 🚀 Быстрая проверка (самый простой способ)

### 1. Через PowerShell (рекомендуется)

```powershell
.\check-railway-logs.ps1
```

Это покажет последние 50 строк логов.

### 2. Просмотр в реальном времени

```powershell
.\watch-railway-logs.ps1
```

Показывает логи в реальном времени (как в терминале). Нажми `Ctrl+C` для выхода.

### 3. Проверка статуса через браузер

```powershell
.\check-railway-status.ps1
```

Введи URL твоего Railway сервиса (например: `https://stunning-manifestation.railway.app`).

---

## 🔍 Что искать в логах

### ✅ Если всё работает правильно, увидишь:

```
🚀 Telegram Bot Scheduler запущен
📅 Текущая дата: 03.11.2025
⏰ Текущее время (МСК): ...
💬 Chat ID: -1003107822060
🔗 Supabase URL: ✓ Настроен
✅ Сервер запущен на порту 3000
🔗 Health check: http://localhost:3000/health
🔗 Test Telegram: http://localhost:3000/test
```

### ❌ Если НЕ работает, увидишь:

```
/app/index.html:1
SyntaxError: Unexpected token '<'
```

Это означает, что Railway пытается запустить `index.html` вместо `server/index.js`.

---

## 🌐 Проверка через браузер

1. Открой URL твоего Railway сервиса + `/health`:
   - Например: `https://stunning-manifestation.railway.app/health`

2. Должен вернуться JSON:
   ```json
   {
     "status": "ok",
     "date": "03.11.2025",
     "time": "...",
     "chat_id": "-1003107822060",
     "supabase_configured": true
   }
   ```

3. Если видишь "Not Found" или ошибку — сервер не запустился.

---

## 📝 Полезные команды Railway CLI

```powershell
# Последние 50 строк логов
railway logs --tail 50

# Логи в реальном времени
railway logs --follow

# Проверить переменные
railway variables

# Проверить статус
railway status
```

---

## 🆘 Если всё красное

1. Запусти `.\check-railway-logs.ps1`
2. Скопируй последние строки логов
3. Посмотри, есть ли там `SyntaxError: Unexpected token '<'`
4. Если есть — значит Railway всё ещё запускает `index.html`

**Решение:** Нужно установить команду запуска через Railway веб-интерфейс или CLI.


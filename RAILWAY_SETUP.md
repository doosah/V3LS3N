# 🔧 Настройка Railway - Пошаговая инструкция

## Проблема
Railway пытается запустить `index.html` как JavaScript, потому что не знает, что нужно запускать сервер из папки `server`.

## Решение

### Способ 1: Через настройки сервиса (самый простой)

1. Открой проект на Railway
2. Нажми на название сервиса (или настройки)
3. Найди раздел **"Settings"** или **"Configuration"**
4. Ищи одну из этих опций:
   - **"Start Command"** или **"Command"** - установи: `cd server && npm start`
   - **"Working Directory"** или **"Root Directory"** - установи: `server`

### Способ 2: Пересоздать сервис

1. В Railway открой проект
2. Удали текущий сервис (или создай новый)
3. Нажми **"New"** → **"Service"** → **"GitHub Repo"**
4. Выбери репозиторий V3LS3N
5. При создании выбери:
   - **Source**: GitHub repo
   - **Branch**: main (или master)
6. После создания перейди в настройки сервиса
7. Найди **"Start Command"** и установи: `cd server && npm start`

### Способ 3: Использовать файлы конфигурации (уже созданы)

Я создал несколько файлов конфигурации:
- `railway.json` - конфигурация Railway
- `nixpacks.toml` - конфигурация Nixpacks
- `Procfile` - для Heroku-совместимых платформ
- `package.json` в корне - с командой запуска

Railway должен автоматически использовать эти файлы.

### Если не работает

1. **Проверь логи Railway** - что именно пытается запустить
2. **Попробуй вручную установить команду**:
   - В настройках сервиса найдите поле "Command" или "Start Command"
   - Установи: `cd server && npm start`
3. **Или используй абсолютный путь**:
   - `node server/index.js`

## Что проверить

После настройки в логах Railway должно быть:
```
🚀 Telegram Bot Scheduler запущен
✅ Сервер запущен на порту 3000
```

Если видишь ошибку `SyntaxError: Unexpected token '<'` или `/app/index.html:1`, значит команда запуска не настроена правильно.

## Где найти настройки

В Railway интерфейс может отличаться, но обычно настройки находятся:
- В боковом меню: **Settings** → **Service**
- Или в верхней панели: **⚙️ Settings**
- Или при клике на сервис: **"..."** → **Settings**

Ищи поле с названием типа:
- "Start Command"
- "Command"
- "Run Command"
- "Start"
- "CMD"

И установи туда: `cd server && npm start`


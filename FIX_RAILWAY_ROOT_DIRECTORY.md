# 🔧 КАК ИСПРАВИТЬ Railway - Root Directory

## ❌ Проблема
Railway пытается запустить `index.html` вместо `server/index.js`

## ✅ Решение (через веб-интерфейс Railway)

### Шаг 1: Открой Railway Dashboard
1. Перейди на: https://railway.app
2. Войди в свой аккаунт

### Шаг 2: Выбери проект
1. Найди проект `stunning-manifestation`
2. Кликни на него

### Шаг 3: Выбери сервис
1. В списке сервисов найди свой сервис (обычно называется как проект)
2. Кликни на него

### Шаг 4: Открой Settings (Настройки)
1. Вверху страницы найди вкладку **"Settings"** (Настройки)
2. Кликни на неё

### Шаг 5: Установи Root Directory
1. Прокрути вниз до секции **"Root Directory"**
2. В поле введи: **`server`**
3. Нажми **"Save"** или **"Update"**

### Шаг 6: Установи Start Command (если есть такое поле)
1. Найдите поле **"Start Command"** (если оно есть)
2. Введи: **`node index.js`**
3. Нажми **"Save"**

### Шаг 7: Дождись перезапуска
1. Railway автоматически перезапустит сервис
2. Подожди 2-3 минуты
3. Проверь логи: `.\check-railway-logs.ps1`

---

## 📸 Где найти Root Directory в Railway UI

В настройках сервиса (Settings) ищи:
- **Root Directory** — поле для ввода пути к корневой директории
- Или **Working Directory** — то же самое
- Или **Base Directory** — тоже самое

Обычно это поле находится в секции **"Build & Deploy"** или **"Deployment"**

---

## ✅ Проверка что всё работает

После установки Root Directory, в логах должно появиться:

```
🚀 Telegram Bot Scheduler запущен
✅ Сервер запущен на порту 3000
```

Вместо ошибки:
```
/app/index.html:1
SyntaxError: Unexpected token '<'
```

---

## 🆘 Если не можешь найти Root Directory

1. Попробуй найти вкладку **"Variables"** или **"Environment Variables"**
2. Добавь переменную:
   - Key: `RAILWAY_ROOT_DIRECTORY`
   - Value: `server`
3. Или попробуй через CLI (но это может не сработать)

---

## 📝 Альтернатива: через Railway CLI (если поддерживается)

```powershell
railway variables --set "RAILWAY_ROOT_DIRECTORY=server"
```

Но лучше делать через веб-интерфейс!


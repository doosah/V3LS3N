# 🔧 Исправление Railway - Нужно установить команду вручную

## Проблема
Railway всё ещё пытается запустить `index.html` вместо `server/index.js`. Конфигурационные файлы не применились автоматически.

## Решение - Установить команду вручную

### Шаг 1: Открой настройки сервиса

1. В Railway открой проект
2. Нажми на название сервиса (например, "stunning-manifestation")
3. Перейди в раздел **"Settings"** (или нажми на иконку ⚙️)

### Шаг 2: Найди поле "Start Command"

В настройках ищи одно из этих полей:
- **"Start Command"**
- **"Command"**
- **"Run Command"**
- **"CMD"**
- **"Start"**

Обычно это поле находится в разделе:
- **"Deploy"** или
- **"Service"** или
- **"Configuration"**

### Шаг 3: Установи команду запуска

В найденное поле вставь:
```
cd server && npm start
```

Или попробуй:
```
node server/index.js
```

### Шаг 4: Сохрани изменения

Нажми **"Save"** или **"Deploy"** - Railway автоматически перезапустит сервис.

### Шаг 5: Проверь логи

После перезапуска в логах должно появиться:
```
🚀 Telegram Bot Scheduler запущен
✅ Сервер запущен на порту 3000
```

## Альтернативный вариант: Пересоздать сервис

Если не можешь найти поле "Start Command":

1. **Удали текущий сервис** (или создай новый)
2. **Создай новый сервис**: New → Service → GitHub Repo
3. При создании обрати внимание на поля:
   - **"Root Directory"** - оставь пустым или установи `server`
   - **"Start Command"** - установи `npm start` (если root directory = server) или `cd server && npm start`

## Проверка

После исправления:
1. Проверь логи - должно быть `✅ Сервер запущен на порту 3000`
2. Открой `/health` - должен вернуться JSON, а не просто "OK"
3. Открой `/test` - должна отправиться тестовая сообщение в Telegram

